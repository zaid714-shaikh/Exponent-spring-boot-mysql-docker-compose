spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.D
    url: jdbc:mysql:localhost:3306/sbms?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
